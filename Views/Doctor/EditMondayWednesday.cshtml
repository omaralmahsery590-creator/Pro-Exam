@model pro_exam.ViewModel.DoctorScheduleViewModel

<h2>Edit Monday, Wednesday Schedule</h2>
<form asp-action="SaveMondayWednesday" method="post">
    @Html.HiddenFor(model => model.DoctorId)
    <table class="table table-bordered">
        <thead>
            <tr>
                <th>Day</th>
                <th>Start Time</th>
                <th>End Time</th>
            </tr>
        </thead>
        <tbody>
            @for (int i = 0; i < Model.MondayWednesdaySchedules.Count; i++)
            {
                <tr>
                    <td>
                        <select asp-for="MondayWednesdaySchedules[@i].Day" class="form-control">
                            <option value="Monday">Monday</option>
                            <option value="Wednesday">Wednesday</option>
                        </select>
                    </td>
                    <td>
                        <input asp-for="MondayWednesdaySchedules[@i].StartTime" type="time" class="form-control" />
                    </td>
                    <td>
                        <input asp-for="MondayWednesdaySchedules[@i].EndTime" type="time" class="form-control" />
                    </td>
                </tr>
            }
        </tbody>
    </table>
    <button type="submit" class="btn btn-primary">Save Changes</button>
</form>
